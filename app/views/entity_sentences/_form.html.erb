<%= form_with(model: entity_sentence, local: true) do |form| %>
  <% if entity_sentence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entity_sentence.errors.count, "error") %> prohibited this entity_sentence from being saved:</h2>

      <ul>
      <% entity_sentence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :entity_id %>
    <%= form.number_field :entity_id %>
  </div>

  <div class="field">
    <%= form.label :source_id %>
    <%= form.number_field :source_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
